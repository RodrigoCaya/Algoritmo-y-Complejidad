2020-1
Tarea 2 Algoritmos y Complejidad

Esta tarea consistia en la realizacion de 1 pregunta que se realiza con Algoritmo Greedy.

Requisitos:
        - Python3

Ejecutar:
--Pregunta
        -El archivo de entrada debe llamarse "input.txt", el cual debe ubicarse en este mismo directorio.
        -Se utiliza "make tarea2" para correr el codigo.
        -El archivo de salida se llama "output.txt", el cual se ubicará en este mismo directorio.
        *De forma opcional se encuentra un archivo para generar inputs de forma aleatoria, este se corre con "make generar" (con archivo salida "input.txt" en este mismo directorio).
