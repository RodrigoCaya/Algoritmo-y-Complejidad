2020-1
Tarea 1 Algoritmo y Complejidad

Integrantes:
Rodrigo Cayazaya 201773538-4
Lucio Fondon 201773610-0
Francisco Reyes 201773529-4

Esta tarea consistia en la realizacion de 3 preguntas, las primeras dos se realizaban con algoritmos Fuerza Bruta y la tercera con el algoritmo Dividir y Conquistar.

Requisitos:
        - Python3

Ejecutar:
--Pregunta 1
        -El archivo de entrada debe llamarse "input.txt", el cual debe ubicarse en Pregunta_1/input.txt.
        -Se utiliza "make pregunta1" para correr el codigo.
        -El archivo de salida se llama "pregunta1.txt", el cual se ubicara en este mismo directorio.

--Pregunta 2
        -El archivo de entrada debe llamarse "input.txt", el cual debe ubicarse en Pregunta_2/input.txt.
        -Se utiliza "make pregunta2" para correr el codigo.
        -El archivo de salida se llama "pregunta2.txt", el cual se ubicara en este mismo directorio.

**Pregunta 3
--Pregunta 3.1
        -El archivo de entrada debe llamarse "input1.txt", el cual debe ubicarse en Pregunta_3/3.1/input1.txt.
        -Se utiliza "make pregunta3.1" para correr el codigo.
        -El archivo de salida se llama "pregunta3.1.txt", el cual se ubica en este mismo directorio.
        *De forma opcional se encuentra un archivo para generar matrices de forma aleatoria, este se corre con "make matriz3.1" (con archivo salida "input1.txt" en este mismo directorio).

--Pregunta 3.2
        -El archivo de entrada debe llamarse "input2.txt", el cual debe ubicarse en Pregunta_3/3.2/input2.txt.
        -Se utiliza "make pregunta3.2" para correr el codigo.
        -El archivo de salida se llama "pregunta3.2.txt", el cual se ubica en este mismo directorio.
        *De forma opcional se encuentra un archivo para generar matrices de forma aleatoria, este se corre con "make matriz3.2" (con archivo salida "input2.txt" en este mismo directorio).